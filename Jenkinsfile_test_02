pipeline {
    agent any 
    stages {
        stage('Checkout') {
            steps {
                checkout scm

            }
        }
        stage('Hello Jenkins') {
            steps {
                echo 'hello Jenkins file 2'
                sh 'uname -n'
                sh 'which jenkins'
            }
            }
        }
    }
